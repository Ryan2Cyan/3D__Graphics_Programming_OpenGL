#include "Collider.h"
#include "CollisionPoints.h"

//// Calculate distance to plane point in space:
//float Collider::DistanceToPlane(const glm::vec3& norm, const glm::vec3& point,
//	glm::vec3& plane_point) {
//
//	return glm::dot((point - plane_point), norm);
//}